# Technical Debt Registry

Full history of all identified technical debt for HomeSync. Format: ID | Description | Severity | Priority | Proposed Solution | Status | Owner | Created

For a short list of what's still open, see the "Currently open TDs" table in [CLAUDE.md](../CLAUDE.md#-technical-debt-registry).

**Priority** (added 2026-08-16 backlog grooming) is severity weighted by current impact frequency/blast-radius — it can diverge from Severity (e.g. TD-010's Medium severity gets High priority because backup loss is catastrophic if it ever hits, despite being rare). It is only set for still-open rows; resolved/closed rows show `—`. The table below is split into two sections: open items sorted High → Medium → Low, then resolved/closed items in their original chronological order.

| ID | Description | Severity | Priority | Proposed Solution | Status | Owner | Created |
|----|-------------|----------|----------|-------------------|--------|-------|---------|
| — | **Open / actionable — sorted High → Medium → Low.** | — | — | — | — | — | — |
| TD-010 | No database backups | Medium | High | Configure MongoDB Atlas backups | Planned (Phase 3) | TBD | 2026-08-08 |
| TD-057 | Offline queue loses an update/delete whose create synced in an earlier batch. `TaskRepository.syncPendingOperations` builds `idRemap` (local id → server id) as a LOCAL variable, rebuilt from scratch on every call. When a queued create succeeds its op is removed from the queue and the remap entry exists only for the remainder of that loop; if a later operation then hits a network-shaped failure the loop `break`s and the remap is discarded. On the next sync pass the create is no longer in the queue, so the still-queued update/delete resolves `entityId` to its original `local-<uuid>` and issues `PATCH`/`DELETE /tasks/local-<uuid>`. The server answers 404 — a genuine 4xx, not offline-worthy — so it burns the 3-retry budget and is then dropped with a Sentry report, silently losing a write the user made offline and was told would sync ("Guardado offline, se sincronizará cuando haya conexión"). `ShoppingRepository` has the same structure. | High | High | Persist the mapping instead of keeping it in a local: when a create resolves, rewrite every queued operation still referencing the local id via `CacheService.updatePendingOperation` (and re-key the cached entity) before removing the create from the queue, so the remap survives across sync passes and app restarts. Add a test that syncs a create, forces a network failure on the following update, then re-runs `syncPendingOperations` and asserts the update targets the server id. | Open | TBD | 2026-08-17 |
| TD-001 | Members embedded in Household document | High | Medium | Migrate to separate HouseholdMember collection | Planned (Phase 2) | TBD | 2026-08-08 |
| TD-007 | No optimistic updates in frontend | Medium | Medium | Implement optimistic UI updates | Planned (Phase 2) | TBD | 2026-08-08 |
| TD-013 | Recurrence computed in UTC without household timezone | Medium | Medium | Add household.timeZone + TZ-aware recurrence | Planned (Phase 2) | TBD | 2026-08-10 |
| TD-024 | SHA-256 migration: raw refresh tokens already persisted in Atlas will not match sha256 lookups after deploy | High | Medium | One-time action: clear refreshtokens collection when deploying b2c481e (safe now: pre-production, no real users; post-user-acquisition this would require a grace-period lookup) | Script ready (scripts/migrate-refresh-tokens.ts); run with --yes during the deploy window of b2c481e | TBD | 2026-08-10 |
| TD-034 | No deploy-order safety net between backend and Flutter app (versioned /health check). CI verification mitigates broken-push risk; deploy-order safety net still pending — CI (TD-008) surfaces a broken backend/frontend push before or alongside Railway's auto-deploy (which triggers on push regardless of CI outcome; no branch-protection gate ties the two together), it does not itself prevent an already-deployed backend from being incompatible with the app version still in users' hands | Medium | Low | Add API version to /health and a client-side check that shows "update the app" when incompatible; requires API versioning (Phase 3) | Planned (Phase 3) | TBD | 2026-08-10 |
| TD-039 | Offline conflict resolution uses last-write-wins; concurrent edits on multiple devices can overwrite | Low | Low | Evaluate CRDT or OT if user reports lost edits | Deferred (Phase 2, if conflicts become frequent) | TBD | 2026-08-11 |
| TD-040 | flutter test hangs on loaded hosts — confirmed (2026-08-13) to reproduce even running test/widgets/offline_banner_test.dart alone, not only when combined with the rest of the suite as first documented. `frontend_server_aot`'s CPU time froze completely (observed stuck at a fixed value for 200s+) even after clearing `build/test_cache`'s incremental-compile cache, so it is a toolchain/host-level stall (likely resource pressure — observed alongside load average ~4 and <60MB free pages), not a test-code defect or a stale-cache artifact. task_tile_test.dart and assignee_selector_test.dart both pass cleanly, alone or together; only offline_banner_test.dart triggers it | Low | Low | Root-caused 2026-08-17: wrap the Hive writes in `tester.runAsync()` so the real disk write completes on the real event loop and releases the box lock before tearDown runs. One-line-scoped fix in the test itself; no production code change needed. | **Resolved (2026-08-17)** — root cause was ours, not a host/toolchain stall as previously recorded. `offline_banner_test.dart`'s 4th test called `CacheService().addPendingOperation()` (a `Box.put()`, i.e. a real disk write) directly inside `testWidgets`' fake-async zone. The write's completion callback is scheduled on the fake clock, which stops being pumped the moment the test body returns, so the write never finishes and Hive's per-box lock is never released; the next operation on that box — `clearAll()` in `tearDown` — then waits on that lock forever. That is the hang: no assertion failure, no output, 0% CPU until the CI step timeout kills it. Evidence (local, idle Mac, Flutter 3.44.9): a probe printing around each step showed the body completing (`count=1`) and `tearDown: before clearAll` printing while `after clearAll` never did; the identical probe with the write wrapped in `tester.runAsync()` passed with exit 0. Explains why only this file was ever affected — `cache_service_test.dart` hits the same box with the same writes but uses plain `test()`, which has no fake-async zone. Also explains the earlier reading of "`frontend_server_aot`'s CPU frozen at 0%": compilation had already finished (3 tests ran first), so 0% CPU was the deadlock waiting, not the host starving. Fixed in `offline_banner_test.dart`; all 6 tests pass in <1s. CI's `continue-on-error` step is now redundant but was NOT removed here — changing `ci.yml` needs the owner's approval. | TBD | 2026-08-15 |
| TD-054 | Logout revokes the refresh token only (auth.service.ts:197, a single `deleteOne`); the access token issued alongside it stays valid until it expires on its own, up to 15 minutes later (`JWT_ACCESS_EXPIRES` default, utils/jwt.ts:8). A user who logs out on a lost or shared device therefore keeps an authenticated window on every `/api` route. This is the standard stateless-JWT tradeoff and is arguably working as designed — filed so the exposure window is written down rather than assumed, not as a defect. auth.test.ts:295-310 covers that the refresh token dies at logout; nothing covers the access token outliving it. | Low | Low | Options in increasing cost: shorten `JWT_ACCESS_EXPIRES`; add a per-user `tokensValidFrom` timestamp checked in `authMiddleware`; or a Redis denylist keyed by `jti`. Only worth doing if the threat model starts including shared/lost devices. | Planned | TBD | 2026-08-16 |
| TD-049 | No real Firebase project connected for push notifications (PDR-008) | High | Low | Create a Firebase project, add Android (`com.homesync.app`) + iOS (`com.homesync.app`) apps, place `google-services.json`/`GoogleService-Info.plist` manually (or `flutterfire configure`), apply the google-services Gradle plugin, enable Push Notifications + Background Modes capability in Xcode with an APNs key uploaded to Firebase, set `FIREBASE_SERVICE_ACCOUNT` in Railway. | Partially resolved (2026-08-16): config files placed manually by owner in `android/app/` and `ios/Runner/` (gitignored); `com.google.gms.google-services` Gradle plugin now applied (`android/build.gradle` classpath + `android/app/build.gradle` apply plugin); `AndroidManifest.xml` already had `POST_NOTIFICATIONS`; `ios/Runner/Info.plist` now declares `UIBackgroundModes` (`fetch`, `remote-notification`); `FIREBASE_SERVICE_ACCOUNT` already set in Railway. Still open: Xcode "Push Notifications" capability (generates `Runner.entitlements` + `CODE_SIGN_ENTITLEMENTS` in pbxproj — deliberately not hand-edited, see IMPROVEMENTS.md), APNs key upload, device end-to-end test. Note: `apply plugin: 'com.google.gms.google-services'` is guarded behind `file('google-services.json').exists()` in `android/app/build.gradle` — CI has no config file (gitignored, machine-local) and the unconditional apply broke the `flutter build apk --debug` CI step on first push (fixed same day). Planned (before beta push notifications can work; see Beta release checklist). | TBD | 2026-08-16 |
| — | **Resolved / closed — chronological, no further action needed.** | — | — | — | — | — | — |
| TD-002 | No pagination on list endpoints | High | — | Implement cursor-based pagination | Resolved (implemented before this analysis, verified 2026-08-16): `task.service.ts`'s `listTasks` and `shopping.service.ts`'s equivalent both use opaque forward-only cursors (`hasMore`/`nextCursor`/`total`), matching ADR-008. TD-027 already documented the frontend catching up to the paginated envelope; this entry was simply never flipped to Resolved at the time. | TBD | 2026-08-08 |
| TD-003 | No offline support in frontend | High | — | Implement Hive caching + sync queue | Resolved (commits 1-3): cache-first repositories, offline pending-operations queue with auto-sync on reconnect (ADR-010), offline UI (banner, pending-count badge, per-item sync/deleted indicators) | TBD | 2026-08-08 |
| TD-004 | No input sanitization | High | — | mongo-sanitize at edge + length limits; raw storage per ADR-009 (escape at render if a web client ships) | Resolved (commit 3) | TBD | 2026-08-08 |
| TD-005 | No test coverage (stack not installed) | High | — | Add Jest + Supertest + mongodb-memory-server + bloc_test. Stack installed: Jest + Supertest + mongodb-memory-server (backend, 236 tests); flutter_test + bloc_test (frontend, 192 tests). CI runs the full suite on every PR since TD-008/TD-044. | Resolved | TBD | 2026-08-08 |
| TD-008 | No CI/CD pipeline | Medium | — | Configure GitHub Actions: backend (typecheck/build/test) + frontend (analyze/sharded test/build apk) on every PR and push to main, frontend-ios (simulator build) on main only | Resolved (commit 1) | TBD | 2026-08-08 |
| TD-009 | No error tracking (Sentry not installed) | Medium | — | Integrate Sentry backend + frontend; backend + frontend with no-op fallback; security warnings (refresh replay) routed to Sentry as alert channel | Resolved (commits 1-2) | TBD | 2026-08-08 |
| TD-011 | No resource-level authorization on tasks (any member can delete) | High | — | Creator-or-admin rule for edit/delete; any member can complete tasks and purchase shopping items | Resolved (commit 1) | TBD | 2026-08-10 |
| TD-012 | No ESLint/Prettier with no-console rule | Medium | — | ESLint 9 flat config with @typescript-eslint/no-floating-promises and no-misused-promises as error; Prettier for formatting; npm run lint runs in CI backend job. | Resolved | TBD | 2026-08-10 |
| TD-014 | No idempotency on write POSTs (retry can duplicate) | High | — | Idempotency-Key header + Redis dedupe | Resolved (commit 2) | TBD | 2026-08-10 |
| TD-015 | No express.json payload size limit | Medium | — | Add limit option | Resolved (implemented before this analysis, verified 2026-08-16): `app.ts` calls `express.json({ limit: '100kb' })`, with a comment citing Hard Rule 14. This entry was simply never flipped to Resolved at the time. | TBD | 2026-08-10 |
| TD-017 | constants.dart with hardcoded local backend URL | Low | — | constants.dart uses String.fromEnvironment() with sensible defaults (API_BASE_URL default https://housetask-production.up.railway.app — production, changed from an initial localhost:3000 default by a same-named follow-up commit; ENVIRONMENT default development). README.md documents `flutter run`/`build` commands with `--dart-define` flags for dev and production. No more --assume-unchanged hack. (A `scripts:` key in pubspec.yaml was considered but rejected — vanilla `pub`/`flutter pub` has no such feature, so it would be inert YAML; the real commands live in README.md instead.) | Resolved | TBD | 2026-08-10 |
| TD-019 | pubspec.lock ignored in frontend/.gitignore (non-reproducible builds) | High | — | Remove from .gitignore and commit the lockfile | Resolved (2026-08-10, Parte 0 chore) | TBD | 2026-08-10 |
| TD-020 | Auth rate limiter had no test (skipped under NODE_ENV=test) | Medium | — | createApp({ authRateLimit }) opt-in + dedicated 429 test | Resolved (2026-08-10, commit B) | TBD | 2026-08-10 |
| TD-021 | Mongoose test connection without bufferCommands:false (slow opaque failures) | Low | — | bufferCommands:false + serverSelectionTimeoutMS 5000 in test harness | Resolved (2026-08-10, commit B) | TBD | 2026-08-10 |
| TD-022 | Refresh token replay did not revoke the token family (stolen-token session survived) | High | — | Detect rotated-token replay via valid signature + missing row, revoke all user refresh tokens | Resolved (2026-08-10, commit B). **Modified by TD-050 (2026-08-17):** the "missing row" branch's revocation is now scoped to tokens that predate the replaying request (`createdAt: { $lt: requestStartedAt }`) instead of an unconditional full-family purge — see TD-050 below for why. The signature/userId-mismatch (tampering) branch is untouched and still purges unconditionally. | TBD | 2026-08-10 |
| TD-023 | Refresh tokens stored as raw JWTs, not hashed (this file previously claimed otherwise) | High | — | Store SHA-256 of the token and look up by hash; a DB leak must not yield usable tokens | Resolved (2026-08-10, commit B) | TBD | 2026-08-10 |
| TD-025 | Monthly recurrence anchor bug: dayOfMonth 31 clamps to Feb 28 and never recovers because the next occurrence is computed from the clamped date instead of the rule anchor | High | — | Anchor monthly computation to rule.dayOfMonth; add weekly/monthly/clamp unit tests | Resolved (commit 1) | TBD | 2026-08-10 |
| TD-026 | List sort not backed by a matching compound index (in-memory sort per page) | High | — | Add sort-exact compound indexes on Task and ShoppingItem | Resolved (commit 3) | TBD | 2026-08-10 |
| TD-027 | Frontend repositories broken against paginated backend (data array → object) | Medium | — | Paginated envelope + per-tab filtering (?status= query param) + per-tab ScrollController + total visible in header. Home and Calendar read the unfiltered allTasks bucket; per-tab totals adjusted optimistically on mutations | UX polish completed (commit 1) | TBD | 2026-08-10 |
| TD-028 | Validation scattered across controllers/services; express-mongo-sanitize incompatible with Express 5 | Medium | — | All endpoints validate input with Zod schemas in backend/src/schemas/. validate() middleware in backend/src/middleware/validate.ts returns 400 with formatted details on ZodError. Controllers receive parsed/typed req.body. (express-mongo-sanitize/Express 5 migration is out of scope — see ADR-009; mongo-sanitize is still active, unchanged) | Resolved | TBD | 2026-08-10 |
| TD-029 | Text persisted HTML-escaped during the escaping window remains escaped | Low | — | Won't fix: pre-production, only local test data affected; re-seed if cosmetic noise bothers; a one-off unescape pass would only be justified if a real household existed in the window | Won't fix | TBD | 2026-08-10 |
| TD-030 | Index tests were temporarily downgraded to schema-declaration level while host disk had <500 MB free | Low | — | Host disk freed; listIndexes() built-index assertions restored | Resolved (commit 1) | TBD | 2026-08-10 |
| TD-031 | POSTs carrying Idempotency-Key hang forever when Redis is unreachable (ioredis maxRetriesPerRequest:null queues commands indefinitely) | High | — | commandTimeout configurable (default 2500ms, env REDIS_COMMAND_TIMEOUT_MS) on a dedicated app-only Redis connection; pub/sub connections stay timeout-free to preserve Socket.io adapter stability; fail-open in middleware | Resolved (commit 1) | TBD | 2026-08-10 |
| TD-032 | Socket.io Redis adapter does not catch its own command rejections; Node 24 kills the process on unhandledRejection | High | — | Global unhandledRejection handler that logs and does not exit (Node 24 default would kill the process) | Resolved (commit 1) | TBD | 2026-08-10 |
| TD-033 | Idempotency fail-open not observable: silent log spam during Redis outage, no metric for postmortem duplicate analysis | Medium | — | In-memory fail-open counter + rate-limited log + JSON endpoint; Prometheus hook when APM lands | Resolved (commit 1) | TBD | 2026-08-10 |
| TD-035 | No server-side isRecurring filter; Recurrentes tab removed to avoid local filtering over paginated data | Medium | — | Restored as a bottom-nav tab (🔁 repeat icon, between Tareas and Calendario) without a new backend endpoint: `TaskCubit.loadRecurringTasks` exhaustively walks the existing UNFILTERED (`status=null`) paginated list to completion — the same cache-safe population `TaskFilter.all` already uses — then groups by series (`parentTaskId ?? id`) client-side to pick one representative occurrence per series (pending first, else most recently due completed), sorted by next due date. A real `?isRecurring=true` backend filter remains the more efficient long-term fix for very large households (this walks every page every time the tab loads) and is still a valid Phase 2 follow-up, but current data already suffices for HomeSync's household sizes. | Resolved (commit 1) | TBD | 2026-08-15 |
| TD-036 | Native scaffolding (gradlew, Runner.xcodeproj, res/, Assets.xcassets, etc.) was hand-curated and incomplete — the repo could not build without running flutter create manually first | High | — | Version the full native scaffolding, reconciled against the already-tracked com.homesync.app package/bundle id; template xcconfig files (Debug/Release) tracked so fresh clones build without flutter create; generated artifacts ignored | Resolved (commit 1) | TBD | 2026-08-11 |
| TD-037 | Sentry hardening: structured captureException tags (category/route/userId/householdId, see 🔔 Sentry Error Tracking & Alerts) on the backend 5xx handler, Mongo connection errors, socket auth/room failures, and economy grant failures; frontend breadcrumbs on login/task-complete/pet flows; dashboard alert-configuration guide | Medium | — | Configure the 2-3 alert rules documented in the guide once a real SENTRY_DSN is set in production | Resolved (commit 3) | TBD | 2026-08-15 |
| TD-043 | Sentry hardening leftovers from TD-037's original scope: configurable tracesSampleRate via env, Idempotency-Key in frontend 5xx context for TD-033 correlation, AppError-500 capture test | Low | — | tracesSampleRate is now 0 in development / 0.2 in production on both backend (NODE_ENV) and frontend (ENVIRONMENT dart-define); ApiService._mapDioError attaches the request's Idempotency-Key (when present) to the Sentry `extra` context alongside status/path; sentry.test.ts covers an explicit AppError(status: 500) tagging the same as an unhandled error. "Concurrent-request context tests" from the original note was not part of this round's scope | Resolved (commit 1) | TBD | 2026-08-15 |
| TD-038 | sentry_flutter 8.14.2's Package.swift allows any sentry-cocoa 8.x (`from: "8.46.0"`) while its podspec pins exactly 8.46.0; SPM had resolved 8.58.4, which broke the plugin's Swift build (SentryBinaryImageCache API changed) | High | — | Pinned both Package.resolved files to 8.46.0 matching the podspec; re-pin on every sentry_flutter upgrade until upstream tightens the SPM constraint. Documented in `frontend/README.md`'s Known Issues. Filing the upstream request (against `getsentry/sentry-cocoa`, to tighten the Package.swift range so it can't drift past the podspec again) is a human action — requires a GitHub account and maintainer engagement — and is not something this pipeline can automate; tracked here as a manual follow-up, not automated work. | Resolved (commit 1); upstream report still open (manual follow-up) | TBD | 2026-08-11 |
| TD-041 | Android minSdk 23+ and Flutter pin in CI — Flutter's DependencyVersionChecker enforces errorMinSdkVersion=23 (build-breaking). Project minSdk raised from 21 to 23 (Android 7.0+), accepting <5% market loss for cleaner builds. CI pins Flutter version (not channel: stable) to prevent surprise floor bumps when Flutter stable advances. Toolchain matrix (read from source, 2026-08-14): Flutter pinned `3.44.9` in CI (`.github/workflows/ci.yml`) against a pubspec floor of `>=3.27.0`; Dart SDK `>=3.6.0 <4.0.0`; Gradle `9.1.0` (`frontend/android/gradle/wrapper/gradle-wrapper.properties`); AGP `9.0.1` and Kotlin `2.3.20` (both `frontend/android/settings.gradle`); `minSdkVersion 23` hardcoded in `frontend/android/app/build.gradle` (PDR-005), while `compileSdk`/`targetSdk`/`ndkVersion` stay dynamic via `flutter.*` rather than hardcoded | Low | — | When upgrading Flutter, check minSdk/Gradle/AGP/Kotlin floors and bump deliberately, not reactively | Resolved | TBD | 2026-08-14 |
| TD-042 | Flutter Built-in Kotlin migration — Flutter 3.44+ is migrating to Built-in Kotlin. Plugins that apply Kotlin Gradle Plugin classically (like sentry_flutter 8.x) fail with "Language version 1.6 is no longer supported" when Kotlin is 2.3.20+. sentry_flutter upgraded 8.14.2 → 9.27.0, which drops the hardcoded languageVersion="1.6" override (still applies classic KGP unconditionally, unlike package_info_plus's AGP-major-version guard — residual, not eliminated). No other project dependency (flutter_local_notifications, connectivity_plus, package_info_plus, shared_preferences_android) applies classic KGP | Low | — | Keep sentry_flutter and other Kotlin-using plugins updated to versions that support Built-in Kotlin or Kotlin 2.x with language version 2.0+ | Resolved | TBD | 2026-08-14 |
| TD-044 | CI only ran test/ (top-level) plus 2 hand-picked test/widgets/ files — new widget test files (e.g. pet_page_test.dart, pet_shop_page_test.dart) silently got no CI coverage at all unless someone remembered to add a shard step for them | Medium | — | Single blocking step runs every `*_test.dart` under test/ and test/widgets/ found via `find`, except offline_banner_test.dart (kept isolated with continue-on-error, TD-040); new test files are covered automatically, no workflow edit needed | Resolved (commit 1) | TBD | 2026-08-15 |
| TD-045 | CacheService.saveTasks reemplaza en vez de mergear la caché offline; una carga con filtro de estado puede evictar tareas de otros estados. Descubierto en round TD-035; el código nuevo lo evita usando fetch sin filtro, pero el comportamiento del repository quedaba pendiente de fix. | Medium | — | `CacheService.mergeTasks` upserts by id without touching anything else cached for the household. `TaskRepository.list`'s first-page cache write now branches on `status`: `status == null` (the unfiltered snapshot — Todas/PDR-003 timeline/Recurrentes/Papelera) still calls `saveTasks` (full replace, unchanged); a real status filter (Pendientes/Completadas) now calls `mergeTasks` instead, so visiting one status tab no longer evicts the other's cached tasks. `_listFromCache` (the offline fallback) was already correct — it filters the merged cache by `status` client-side — and is untouched. Known adjacent issue found but deliberately NOT fixed here (out of TD-045's literal scope): `TaskCubit.loadTimeline`'s first page is date-windowed, not a full snapshot either, yet also has `status == null` so it still hits the full-replace path — tracked as a candidate follow-up, not filed as its own TD yet. Checked `ShoppingRepository.list` for the same pattern: it has no `status` query param at all (every call is an unfiltered full-list fetch, filtered only client-side by `ShoppingCubit`), so this specific bug does not apply there today. | Resolved (commit 1) | TBD | 2026-08-15 |
| TD-046 | Tasks were hard-deleted: DELETE /tasks/:id removed the document outright, so an accidental (or malicious) delete was unrecoverable. Filed as TD-046, not "TD-009" — the originating task/branch referred to this as TD-009, but that id already denotes the resolved Sentry integration above; reusing it here would have overwritten that entry instead of adding a new one. | High | — | Soft delete: Task gains `isDeleted` (indexed, default `false`) + `deletedAt`. `listTasks` excludes `isDeleted:true` by default with an opt-in `includeDeleted` param for the trash view; `updateTask`/`completeTask` 404 on a deleted task. DELETE sets `isDeleted`/`deletedAt` instead of removing the document (idempotent no-op on a retry against an already-deleted task); new `POST /tasks/:id/restore` clears both, restricted to the task's creator or a household admin (TD-011's rule). Frontend: `Task.deletedAt` added alongside the pre-existing `isDeleted` (which already covered the offline-pending-delete case — now doubles as the server's real soft-delete flag on an `includeDeleted=true` fetch); new Papelera view reached from an AppBar action on Tareas, listing deleted tasks with a Restaurar button, walking the paginated list client-side and filtering to `isDeleted` rows — same pattern TD-035's Recurrentes tab established, instead of a dedicated backend endpoint. Compound sort indexes (TD-026) were deliberately left unchanged (no `isDeleted` in their key) to keep this change minimal; worth revisiting if a household accumulates enough deleted tasks to matter. | Resolved (commits 1-2) | TBD | 2026-08-15 |
| TD-047 | "Home doesn't show tasks until refresh" hypothesis | Low | — | Investigated 2026-08-16; hypothesis refuted — root causes were two pre-existing bugs (TD-046-related timeline stale + creation 400). Won't fix; regression coverage added. | Resolved | TBD | 2026-08-16 |
| TD-048 | TD-046's soft delete left `isDeleted:true` tasks with no cleanup policy — the `tasks` collection accumulates trash forever, with no automated or manual way to reclaim that space. | Medium | — | Purge policy sharing one query (`taskService.purgeDeletedTasks`: `isDeleted:true, deletedAt: { $lt: now - N days }`) across two entry points: `backend/src/scripts/purge-trash.ts` (`--days N`, default 30, global across every household — meant for a scheduled job, optional Railway cron documented in backend/README.md) and `POST /households/:householdId/tasks/purge?days=` (admin-only, scoped to one household, broadcasts `tasks:purged`). Frontend: "Vaciar papelera" AppBar action in the Papelera view (confirmation dialog, refreshes the list on success) plus an "X tareas antiguas" hint when trash holds entries past the retention window. | Resolved (commit 1) | TBD | 2026-08-16 |
| TD-018 | Member-leave lifecycle not handled: a removed/departed member's pending task assignments kept pointing at them, with no "former member" fallback in the assignee UI. Note: the codebase has no self-service "leave household" endpoint today — `removeMember` (admin-only) is the only membership-exit path, so this resolution attaches the cleanup there rather than to a `leaveHousehold` function, which does not exist. Filing a self-service leave endpoint remains a legitimate gap, tracked as a candidate follow-up (see IMPROVEMENTS notes for this round), but is out of TD-018's literal scope (its Proposed Solution never called for a new endpoint). | High | — | `household.service.ts`'s `unassignDepartedMemberTasks` runs after `removeMember` commits the membership change: it `$pull`s the departed user from `assignedTo` on every PENDING, non-deleted task in the household (completed/deleted tasks are historical record and are left alone), re-broadcasting `task:updated` per affected task so connected clients drop the stale avatar without a manual refresh. Wrapped in try/catch (same fire-and-forget contract as `grantCoins`/`sendPushNotification`) so a cleanup failure never fails the removal itself. Tasks the departing member CREATED are preserved untouched (only their `assignedTo` is pruned, same as any other task) — authorship is never erased. Frontend: `AvatarStack` (used by `TaskTile` for the assignee row) now flags a user as a former member the same way `TaskTile` already flagged a former `completedBy` (PDR-002) — either the id no longer appears in `HouseholdCubit`'s current member list, or the ref failed to populate at all (empty name AND email, e.g. a hard-deleted account) — and renders a generic grey person icon with an "Ex-miembro" tooltip instead of the normal name-initials avatar. Kept in Spanish ("Ex-miembro"), matching the app's existing Spanish-UI convention and the identical completedBy label, rather than the literal English "Former member" string from the originating task description. | Resolved (commit 1) | TBD | 2026-08-16 |
| TD-016 | CORS_ORIGINS empty = * allowed in production | High | — | Fail-fast at startup in production | Resolved (implemented before this round, verified 2026-08-16 in PR #22): `utils/env.ts`'s `validateProductionEnv` throws when `NODE_ENV=production` and `CORS_ORIGINS` is empty/unset (also checks `MONGODB_URI` and both JWT secrets' minimum length in the same pass), called from `server.ts`'s `start()` before `connectDatabase()` so a misconfigured process never binds. Already covered by 5 tests in `env.test.ts`. This entry — like TD-002/TD-015 before it — was simply never flipped to Resolved; no new code was needed this round, only the docs correction (see CLAUDE.md's "Security rules" and the Phase 1 Completion note). | TBD | 2026-08-10 |
| TD-006 | Rate limiting only on auth endpoints | Medium | — | Add global + per-endpoint rate limiting | Resolved (PR #22): `app.ts`'s `buildGlobalLimiter` applies 100 req/15min/IP to every `/api` route via `express-rate-limit` (already a dependency for the existing auth-only limiter), mounted after Swagger docs (so browsing `/api/docs` is never rate-limited) and before the route table. `/api/auth/*` is exempted via `skip: (req) => req.originalUrl.startsWith('/api/auth')` so those requests are never double-counted against both the global and the existing 5/15min auth-specific limiter. Off by default under `NODE_ENV=test` (`CreateAppOptions.globalRateLimit`), same opt-in pattern as the pre-existing `authRateLimit` option, since most suites fire far more than 100 requests against one shared app instance across their `it()` blocks. Covered by `security-hardening.test.ts` (101st request → 429 with the documented envelope; `/api/auth/*` confirmed exempt). | TBD | 2026-08-08 |
| TD-050 | Concurrent refresh with the same token revoked the whole token family, logging the legitimate client out (spurious logout). `refresh()` claims the row atomically with `findOneAndDelete` (auth.service.ts); the loser of a concurrent race saw `!stored` and could not distinguish "benign concurrent double-submit" from "replayed stolen token", so it called `revokeAllUserTokens` unconditionally — deleting the brand-new row the winner had just created in `generateTokens`. The winner's 200 response then handed the client a refresh token whose DB row was already gone, and its next refresh 401'd. Reachable in practice on mobile: a request the client retries after a timeout (the first attempt having already succeeded server-side) presents the same token twice. | High | — | Scope the replay revocation to tokens that predate the replaying request — `deleteMany({ userId, createdAt: { $lt: requestStartedAt } })` — so a concurrent legitimate rotation's brand-new token survives while a genuine replay is still fully revoked. | **Resolved (2026-08-17, fix Opción A aprobado por dueño):** `revokeAllUserTokens` now takes an optional `createdBefore` timestamp; the "missing row" replay branch in `refresh()` (auth.service.ts) passes a `requestStartedAt` captured before any async work, so the revocation query becomes `deleteMany({ userId, createdAt: { $lt: requestStartedAt } })`. A concurrent legitimate rotation's winner creates its new token AFTER every racing request's `requestStartedAt`, so it survives; a genuine replay (hours/days later) still predates the replaying request and is still fully revoked — the sequential-replay test (auth.test.ts) is unchanged and still passes. Fix implemented: `deleteMany` filtered by `createdAt < requestStartedAt`. Decisión del dueño: se prioriza UX (evitar logout fantasma con conexión inestable) sobre detección estricta de replay en una ventana de microsegundos — un atacante necesitaría ganar una carrera dentro del tiempo que tarda este handler en ejecutarse, no simplemente reproducir el token más tarde. New test added covering 3 concurrent `/refresh` calls with the same token: exactly one winner, no phantom logout, DB-level assertion that only the winner's token survives the race. See also TD-022 (the branch this modifies) and IMPROVEMENTS.md. | TBD | 2026-08-17 |
| TD-051 | `jwt.verify` was called without pinning `algorithms` (utils/jwt.ts), so the accepted algorithm set was whatever jsonwebtoken defaulted to rather than an explicit allowlist declared by this codebase. Not exploitable on the installed jsonwebtoken ^9.0.2 (a string/Buffer secret already restricts it to the HMAC family), but the protection lived in a transitive dependency's default rather than in our own call. | Medium | — | Pass the algorithm allowlist explicitly to both `jwt.sign` and `jwt.verify` instead of relying on jsonwebtoken's default, plus tests asserting a token signed with another algorithm is rejected. | **Resolved (2026-08-17):** protección hecha explícita en código, ya no depende de defaults de dependencias. `utils/jwt.ts` now defines `JWT_ALGORITHM = 'HS256'` and passes it explicitly as `{ algorithm: 'HS256' }` to both `jwt.sign` calls (`signAccessToken`/`signRefreshToken`) and `{ algorithms: ['HS256'] }` to both `jwt.verify` calls (`verifyAccessToken`/`verifyRefreshToken`). Covered by two new tests in `auth.test.ts`: a refresh token and an access token each signed HS384 with the correct secret are both rejected (401) via the real `/api/auth/refresh` and `/api/auth/me` endpoints, while a correctly signed HS256 token still succeeds. | TBD | 2026-08-17 |
| TD-052 | `validateProductionEnv` (utils/env.ts) enforced a 32-character minimum on `JWT_SECRET` and `JWT_REFRESH_SECRET` independently, but never asserted the two DIFFER. If a deploy set both to the same value, a refresh token would verify fine as an access token too. Config-dependent, not a live defect in any environment observed. | Medium | — | Add a `JWT_SECRET !== JWT_REFRESH_SECRET` check to `validateProductionEnv`'s `problems` list, covered by a test. | **Resolved (2026-08-17):** protección hecha explícita en código, ya no depende de defaults de dependencias. `validateProductionEnv` now pushes `'JWT_SECRET and JWT_REFRESH_SECRET must not be the same value'` onto its `problems` list whenever both are set and equal (checked after the individual length checks, so it never double-reports a too-short secret as also "identical"). Covered by a new test in `env.test.ts` (throws when both secrets are the valid-but-identical `'a'.repeat(32)`); the pre-existing "everything valid" test was updated to use two distinct valid secrets so it doesn't trip the new check itself. | TBD | 2026-08-17 |
| TD-053 | Login was an account-enumeration oracle by TIMING despite returning identical messages. `login` (auth.service.ts) returned as soon as `findOne` missed, but paid a full `bcrypt.compare` (~100ms) when the address existed — so a registered email was distinguishable from an unregistered one by response latency alone, even though Hard Rule 2's identical-status/identical-message requirement was correctly met. | Medium | — | Compare against a fixed dummy bcrypt hash when the user is not found so both branches pay the same cost; assert the dummy compare is invoked rather than timing by wall clock. | **Resolved (2026-08-17):** protección hecha explícita en código, ya no depende de defaults de dependencias. `auth.service.ts` now computes `DUMMY_PASSWORD_HASH` once at module load (`bcrypt.hashSync` of a fixed non-secret plaintext) and `login`'s miss path (`!user`) calls `bcrypt.compare(password, DUMMY_PASSWORD_HASH)` before throwing, so both branches pay the same bcrypt cost. Verified manually that a dummy compare and a real-hash compare take the same order of magnitude (~80ms each at BCRYPT_ROUNDS=10) — not asserted by wall-clock in the test suite (would be flaky in CI, as the original proposed solution noted) but by a new test in `auth.test.ts` spying on `bcrypt.compare` and asserting it is invoked exactly once on the miss path. `BCRYPT_ROUNDS` was already an explicit module-level constant before this round (auth.service.ts:12) — no change needed there, only the miss-path compare was added. | TBD | 2026-08-17 |
| TD-055 | Session expiry never routed the user back to login. `ApiService._onError` clears the stored tokens and calls `onSessionExpired` when a refresh fails, and `AuthCubit.onSessionExpired` duly emitted `AuthStatus.unauthenticated` — but the only widget listening for that status was `SplashPage`, which `pushReplacementNamed(Routes.main)` removes from the tree at startup. `app.dart`'s global `BlocListener` filtered on `authenticated` only, `MainScaffold` never listened to `AuthCubit`, and `routes.dart` had no auth guard. A refresh failure mid-session left the user sitting on the main shell with no tokens. | High | — | Add a global `BlocListener<AuthCubit, AuthState>` in `app.dart`, above `MaterialApp`, that on `unauthenticated` disconnects the socket, resets the feature cubits (TD-058) and navigates via `Routes.navigatorKey`. | **Resolved (2026-08-17):** extracted into `presentation/widgets/session_listeners.dart`'s `SessionListeners` widget (wraps `MaterialApp` in `app.dart`, replacing the old single-purpose push-notification `BlocListener`), so a test can exercise it directly against fake cubits instead of standing up the real composition root. Its `unauthenticated` listener (fires only on the transition INTO that status, `listenWhen: previous.status != current.status && current.status == unauthenticated`) disconnects `SocketCubit` (which also clears `_listenersBound`, incidentally fixing the F6 rebind trap noted in the round-2 scan — see IMPROVEMENTS.md), resets every domain cubit (TD-058), and navigates via `Routes.navigatorKey.currentState?.pushNamedAndRemoveUntil(Routes.login, ...)` — the same precedent `NotificationService` already uses for context-free navigation. `SplashPage`'s own listener was narrowed to the `authenticated` branch only (the `unauthenticated` → login case is now the global listener's job); `ProfilePage._logout` was simplified to just call `AuthCubit.logout()`, since the teardown/navigation it used to do by hand for `HouseholdCubit` alone now happens centrally for all five domain cubits. Covered by 2 of the 3 new tests in `test/widgets/session_listeners_test.dart`. | TBD | 2026-08-17 |
| TD-058 | Task, Shopping, Pet and Stats cubits kept the previous account's data in memory across logout. `profile_page._logout` reset only `HouseholdCubit` (`reset()` existed on that cubit alone); `AuthCubit.logout()` wipes the Hive boxes via `CacheService.clearAll()`, but nothing cleared the equivalent in-memory `TaskState.buckets`/`timelineDays`, `ShoppingState.items` or `PetState.pet`. On the next login, account A's task titles, shopping items and pet would render under account B for the duration of the first network round trip (or for the whole session, if account B had no household). | High | — | Add a `reset()` to `TaskCubit`, `ShoppingCubit`, `PetCubit` and `StatsCubit`, called from the same global listener TD-055 introduces. | **Resolved (2026-08-17):** `reset()` added to `TaskCubit`, `ShoppingCubit`, `PetCubit` and `StatsCubit` — each emits its `const` initial state and clears its private household/user id field(s) so a subsequent `load()` is a genuine fresh fetch, not a stale-guarded no-op. `HouseholdCubit.reset()` already existed (pre-dates this TD) and got a cross-referencing doc comment. All five are now called from `SessionListeners` (TD-055) on every `unauthenticated` transition — both the explicit-logout path and the session-expiry path, not just the logout button as before. Unit tests: `TaskCubit.reset` (×2, `task_cubit_test.dart`), `ShoppingCubit.reset` (`shopping_cubit_test.dart`), `PetCubit.reset` (×2, `pet_cubit_test.dart`); integration coverage for all five together (plus the socket) in `test/widgets/session_listeners_test.dart`. | TBD | 2026-08-17 |
| TD-056 | `TaskState.timelineCursor` was silently reset to null by nearly every `emit`. `TaskState.copyWith` assigned `timelineCursor: timelineCursor` unconditionally, but only 2 of the ~18 emit sites in `task_cubit.dart` re-passed it: `loadTimeline` and `loadMoreTimeline`'s success paths. `load`, `loadMore`, `syncPending`, `clearOfflineNotice`, `loadRecurringTasks`, `loadTrashTasks`, `restoreTask`, `_upsert`, `_remove` and every `on Failure` handler all dropped it. Consequence: `loadMoreTimeline`'s `continuingWithinWindow` guard (`timelineHasMore && timelineCursor != null`) was effectively always false, so it always widened the window instead of draining page 2+ of the current one. Same root cause silently wiped `timelineError`/`recurringError`/`trashError` too (F7 in IMPROVEMENTS.md) — `clearOfflineNotice()`, meant to touch only `offlineNotice`, wiped all three as a side effect. | High | — | Replicate the `clearError`-style sentinel `StatsCubit`/`HouseholdCubit`/`AuthCubit` already use: `timelineCursor: clearTimelineCursor ? null : (timelineCursor ?? this.timelineCursor)`. Add a regression test that interleaves an unrelated emit between `loadTimeline` and `loadMoreTimeline` and asserts the cursor survives. | **Resolved (2026-08-17), closes F7 too:** `TaskState.copyWith` now takes a `clearX` bool (default `false`) for every sticky nullable field — `error`, `timelineCursor`, `timelineError`, `recurringError`, `trashError` — each becoming `clearX ? null : (x ?? this.x)`; omitted, they now preserve. `offlineNotice` is the sole deliberate exception, left unconditional (it is genuinely one-shot by design, see its doc comment) — `clearOfflineNotice()` is now literally `emit(state.copyWith())`, so it touches nothing else. Four call sites that used to pass an explicit `field: null` to clear at the start of an operation (`load`, `loadTimeline`, `loadRecurringTasks`, `loadTrashTasks`) now pass the matching `clearX: true` instead. `loadTimeline`/`loadMoreTimeline`'s success emits pass `clearTimelineCursor: result.nextCursor == null` alongside `timelineCursor: result.nextCursor`, since a legitimately-null "no more pages" cursor must still overwrite, not be treated as "not specified" — the same distinction `TaskBucket.nextCursor` already handles by convention (every call site re-passes it), kept as-is and out of this fix's scope. F11: the pre-existing `loadMoreTimeline` cursor test called `loadTimeline()`/`loadMoreTimeline()` back-to-back with no intervening emit, hiding the bug — now interleaves `clearOfflineNotice()` between them and asserts the cursor survives. New tests: `TaskState.copyWith` with no args preserves every sticky field (only `offlineNotice` clears); each `clearX` flag nulls only its own field; `clearOfflineNotice` leaves `error`/`timelineCursor`/`timelineError`/`recurringError`/`trashError` untouched. All 228 frontend tests green (`flutter test`, Flutter 3.44.9), `flutter analyze` clean. | TBD | 2026-08-17 |
